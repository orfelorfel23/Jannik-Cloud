#!/usr/bin/env bash
set -euo pipefail

DATA_DIR="/mnt/Jannik-Cloud-Volume-01/quizalarm/data"

echo "[quizalarm] Initializing directories..."
mkdir -p "${DATA_DIR}"
chmod 777 "${DATA_DIR}" || true

echo "[quizalarm] Init complete."