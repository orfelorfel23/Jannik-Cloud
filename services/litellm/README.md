# LiteLLM — LLM Proxy Gateway

## Subdomain
`llm.orfel.de`

## Port Mapping
`556:4000`

## Dependencies
- PostgreSQL (DB: `litellm_db`)

## Configuration
No external API keys in `.env`. Configure LLM providers via the LiteLLM admin UI after deployment.

## Persistent Data
`/mnt/Jannik-Cloud-Volume-01/litellm/`
