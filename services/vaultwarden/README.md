# Vaultwarden — Password Manager

## Subdomain
`pw.orfel.de`

## Port Mapping
`7277:80`

## Dependencies
- PostgreSQL (DB: `vaultwarden_db`)

## Admin Panel
Access at `https://pw.orfel.de/admin` using the `ADMIN_TOKEN` from `.env`.

## Persistent Data
`/mnt/Jannik-Cloud-Volume-01/vaultwarden/`
